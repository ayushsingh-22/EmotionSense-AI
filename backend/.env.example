# Database (Neon Postgres via Prisma)
# Get this from your Neon project dashboard: https://console.neon.tech
# Use the "pooled" connection string for normal app traffic.
DATABASE_URL="postgresql://<user>:<password>@<host>/<db>?sslmode=require"

# Auth
# Must be the SAME value as NEXTAUTH_SECRET in frontend/.env.local — the
# backend uses it to verify the short-lived bearer token the frontend mints
# after a NextAuth sign-in. Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=

# Cron
# Required for GET /api/cron/ping (keeps Neon warm). Generate any random string.
CRON_SECRET=

# --- Everything below is unrelated to the Neon/Supabase migration ---
# Gemini API Configuration (Primary LLM)
GEMINI_API_KEY1=
GEMINI_API_KEY2=

# Groq (LLaMA fallback + Speech-to-Text)
GROQ_API_KEY=

# Text-to-Speech
GOOGLE_TTS_API_KEY=
SARVAM_API_KEY=
MURF_API_KEY=
OPENAI_API_KEY=

# HuggingFace
HUGGINGFACE_API_KEY=

# Email (Nodemailer) — used for emergency contact alerts
EMAIL_ENABLED=false
EMAIL_PROVIDER=
GMAIL_EMAIL=
GMAIL_APP_PASSWORD=

# Server
PORT=8080
NODE_ENV=development
CORS_ORIGIN=*
